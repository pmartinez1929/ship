describe("Ship", function() {
  var motor;
  var damage;
  var extra;

  beforeEach(function() {
    motor = new Motors(100,0);
  });

  describe("Motors", function() {

    it("The engine has a damage", function() {
      //motor.damage(damage);
      //expect(motor.capacity).toBeGreaterThan(damage);
    });

    it("The engine has actived an extra capacity", function() {
      //motor.damage(damage);
      //expect(motor.capacity).toBeGreaterThan(damage);
    });

    it("If the engine has an extra, it live time is reduced", function() {
      //motor.damage(damage);
      //expect(motor.capacity).toBeGreaterThan(damage);
    });

    it("The engine chashes", function(){

    });

  });

  //Ship
  it("Velocity of the ship", function(){

  });

});
